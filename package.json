{
  "name": "param-validator",
  "description": "Library that searches a controller file's comments for required params and returns a 400 with a list of missing params if they're not all supplied.",
  "version": "0.0.7",
  "author": "Tom Roberts",
  "dependencies": {
    "annotation": "0.1.0"
  }
}
